{
    "environments": {
      "test": {
        "scripts": {
          "test-setup": "gem install rubocop",
          "test": "rubocop ."
        },
        "addons": ["heroku-postgresql:in-dyno", "heroku-redis:in-dyno"]
      }
    }
  }